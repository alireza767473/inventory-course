title: 'Inventory '
description: Inventory project is a technology project
template:
  repo: inventory-template
  name: inventory
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
